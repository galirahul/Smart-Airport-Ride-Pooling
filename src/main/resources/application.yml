spring:
  datasource:
    url: jdbc:mysql://localhost:3306/airport_pooling?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: airport_user
    password: airport123

  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: true

server:
  port: 8080
